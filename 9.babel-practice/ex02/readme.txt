2. Babel Cli 
    1) 설치
        $ npm i -D @babel/core @babel/cli
    2) 사용법
        $ npx babel src -o dist

    3) 변환 규칙은 플러그인(plugin) 또는 프리셋(reset) 가지고 있고 babel 설정을 통해 적용한다.
    4) 설치
        $ npm i -D @babel/core